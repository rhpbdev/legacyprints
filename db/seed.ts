// import dotenv from 'dotenv';
// import { drizzle } from 'drizzle-orm/neon-http';
// import { themesTable } from './schema';

// dotenv.config({
// 	path: '.env.local',
// });

// const db = drizzle(process.env.DATABASE_URL!);

// const themesSeedData: (typeof themesTable.$inferInsert)[] = [

// async function main() {
// 	await db.insert(themesTable).values(themesSeedData);
// }

// main();
